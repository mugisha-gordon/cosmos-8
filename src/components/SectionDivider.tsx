const SectionDivider = () => (
  <div className="relative py-8 flex items-center justify-center">
    <div className="w-24 h-px bg-gradient-to-r from-transparent via-water to-transparent" />
    <div className="mx-4 w-2 h-2 rounded-full bg-nexus animate-pulse-glow" />
    <div className="w-24 h-px bg-gradient-to-r from-transparent via-fire to-transparent" />
  </div>
);

export default SectionDivider;
